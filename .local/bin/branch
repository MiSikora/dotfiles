#!/usr/bin/env bash
set -euo pipefail

if [ $# -eq 0 ]; then
  git branch
else
  joined=$(
    IFS=/
    echo "$*"
  )
  username=$(whoami)
  branch_name="${username}/${joined}"
  git checkout -b "$branch_name"
fi
