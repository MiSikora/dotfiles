#!/usr/bin/env zsh
set -e

dir="$(pwd)"

while [[ "$dir" != "/" ]]; do
  p=$(find "$dir" -maxdepth 1 -name "$1" 2>/dev/null)

  if [[ -n "$p" ]]; then
    echo "$p"
    exit 0
  fi

  dir="$(dirname "$dir")"
done

printf "\033[1;31m$1 not found\033[0m\n" 2>&1
exit 1

